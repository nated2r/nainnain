---
description: 當使用者要求新增功能、修改邏輯或添加新檔案時的強制開發流程 (Feature Dev Workflow)
globs: *
alwaysApply: true
---

# 專案開發與功能實作標準操作程序 (SOP)

為維護本專案的架構純潔度並避免技術債堆積，本專案已啟用「高度結構化防呆模式」。當使用者要求「新增功能」、「重構模組」或「建立新檔案」時，**必須嚴格依照以下階段依序進行**。

## 階段零：需求釐清與 AI 轉診評估 (PLAN Initial Phase)
在寫出哪怕是一行程式碼之前，你必須先主動：
1. **【先做功課】查閱專案現狀**：在向使用者發問前，必須先自行查閱現有的專案架構、對話紀錄或相關文件，了解背景，避免問出早已有答案的問題。
2. **釐清需求邊界 (極致白話文提問)**：在掌握現有資訊後，對於不足之處，因為使用者無程式背景，你必須**用最簡單的生活化語言**深入詢問預期的運作邏輯、畫面呈現方式，嚴禁使用工程術語（如 Redux, Middleware, ORM 等）來反問使用者。
3. **多重 AI 轉診評估**：在得知精準需求後，用白話文向使用者解釋此任務適合由誰接手。若更適合其他工具，主動提醒使用者切換，**並且自動生成一段標準化的交接提示詞 (Prompt)** 供其複製貼給目標 AI。這段交接文字必須包含：**「目前的進度/狀況」、「接下來的目標任務」、「目前遇到的困難」** 三大要素。
   > **💡 AI 代稱與協作口訣：「寫扣微調找 cur (Cursor)，架構開荒找 anti (Agent)，終端報錯找 cc (Claude Code)。」**
4. **確認接單**：若評估此任務適合你處理，再進入後續的架構分配階段。

## 階段一：架構分配與請求同意 (PLAN Approval Phase)
確認接下任務後，在動手實作前：
1. **強制資料夾分配**：根據專案現有的目錄結構，**主動建議**相關的程式碼、資源檔、測試檔應該個別放置在哪些明確的資料夾內。
2. **請求同意**：「請問您同意將這些功能模組與檔案放在我上述建議的目錄下嗎？還是您有其他的存放偏好？」
   ⚠️ **鐵律：在使用者給出同意的答覆之前，系統鎖死，絕對不允許開始撰寫或輸出任何實作層級的程式碼！**
3. **時光機備份建議**：在獲得使用者同意準備開工前，必須主動提醒使用者：「**建議您先進行一次 Git Commit 存檔，如果接下來出錯我們才有時光機能還原。**」

## 階段二：測試導向與開發實作 (EXECUTION 模式)
當獲取使用者針對「階段一」的同意後，才可進入此階段：
1. **測試先行 (TDD 思維)**：思考如何驗證此新功能。若專案有既定的測試框架，應優先考慮如何補上對應的測試。
2. **遵守規劃**：將程式碼精準寫入階段一雙方同意的路徑中。一次專注於單一目標，避免無謂地去修改不相干的舊檔案。

## 階段三：內部驗證與防錯 (VERIFICATION 模式)
功能實作完成後，視為主動審查期：
1. **主動執行測試**：必須指導使用者如何測試剛寫好的功能，或提供可直接複製貼上的驗證指令（如 `npm run dev`, `pytest`, ./script 等）。
2. **排除錯誤與強制煞車**：若經驗證有 Bug 或無法預期的副作用，必須優先修復。**但是，如果同一個錯誤連續 3 次都修復失敗，必須立刻觸發煞車機制！** 停止修復動作，向使用者說明目前狀況與失敗原因，請求新的方向，嚴禁盲目瞎猜與浪費資源。

## 階段四：成果交付與後續提醒
驗證通過後：
1. **更新與檢查 README**：在交付前，**必須**檢查並更新專案根目錄的 `README.md`，將新增的功能寫入文件，同時刪除或修正與現有功能矛盾、過時的描述，確保專案移交給他人時文件 100% 正確。
2. **變更清單報告**：簡短列出本次新增/修改的所有檔案之「明確相對路徑」。
3. **運行指示**：如果本次改動需要重啟伺服器、清空快取或執行特定的重啟腳本，請給出明確指令提醒使用者。
