---
description: 啟動專案開發模式並讀取最高規則
---

1. 請先使用 `view_file` 工具讀取專案根目錄下的 `.cursorrules` 或是 `1_公版_cursorrules.md`。
2. 將裡面的「極致白話文模式」與「AI 轉診分工」規則牢牢記在系統提示 (System Prompt) 中。
3. 嚴格自動進入 PLAN 模式的「第零步」，主動向使用者打招呼，並問我：今天想為專案加點什麼功能？（⚠️ 必須使用極度簡單的生活化白話文問我）。
